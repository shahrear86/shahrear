result = function(m,n){(matrix(runif(m*n),m,n))^2 -(matrix(runif(m*n),m,n))^2}
# result(m,n) function will calculate for you (x^2 - y^2), where the value of 
#the x and y are the uniformly randomly chosen value between 0 and 1
#result(10,10) for example
